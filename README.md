# Virtual Band V2

Clean-slate rebuild branch.

Rules:
- Do not import legacy runtime code by default.
- Old branches are reference only.
- Reintroduce assets, models and features one subsystem at a time.
- Every subsystem must have an explicit owner and public interface.

Current state: empty application scaffold.

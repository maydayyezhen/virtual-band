# 项目协作入口

## 演出创作与扩展

新增完整歌曲时先读 [新作品交付流程](docs/NEW_SONG.md)，它覆盖 MIDI、四轨编排、封面、统一注册和验收。

涉及歌曲作品、灯光、导播镜头或 LED 时，先读 [演出创作接口](docs/SHOW_AUTHORING.md)，再按其中索引查看本次相关模块的详细文档和源码。主线页面为 `/studio/band/`。

- 每次编排都考虑灯光、LED、镜头和片头片尾之间的配合；不需要改的轨道可以沿用，但不要漏看其变化时间。
- 公共预设和波西米亚是素材与示例，不是创作上限。允许新增机位、运动、转场路径、灯光技法和屏幕内容源；现有接口无法表达时，明确扩展相应模块并同步文档。
- 作品编排放在作品文件，通用执行能力留在对应模块。沿用统一歌曲时钟、实例绑定和资源生命周期，不在作品中另建音频调度或渲染循环。
- 按实际改动选择指南中的验证入口；只改文档不需要运行完整演出或导出视频。接口变化时同步更新指南，避免留下过期说明。

## Frontend UI Defaults

- 不为了视觉美化随意引入大型依赖；优先使用项目已有设计系统、样式工具、组件库和轻量实现。
- 只调整界面、样式、交互状态和可访问性；不要擅自修改业务逻辑、接口字段、数据结构或后端契约。用户明确授权的架构或接口任务按其授权范围执行。
- 页面完成后，做一次 UI、UX、可访问性和交互状态自查。

## Personal Skills

- 用户已停用个人技能。不要加载或执行 `C:\Users\77349\.codex\memories\skills\` 下的技能，也不要因历史记忆中的推荐而恢复使用。除非用户今后明确要求重新启用。
